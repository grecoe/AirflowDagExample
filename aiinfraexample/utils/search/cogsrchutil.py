import os
from azure.core.credentials import AzureKeyCredential
from azure.search.documents import SearchClient


class CogSearchUtils:
    def __init__(self, instance_name, instance_rg, instance_sub, credentials):
        pass